import React from "react";

function Footer() {
  return (
    <div
      style={{
        height: "70px",
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        justifyContent: "center",
        fontSize: "1rem",
        backgroundColor: "#141c20",
        color: "white",
      }}
    >
      <p>LoL Progress Tracker</p>
    </div>
  );
}

export default Footer;
